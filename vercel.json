{
  "build": {
    "env": {
      "HUGO_VERSION": "0.140.0",
      "HUGO_ENV": "production",
      "HUGO_EXTENDED": "true"
    }
  },
  "buildCommand": "python3 -m pip install -r requirements.txt && python3 setup.py --token $NOTION_TOKEN && python3 notion_hugo_app.py && hugo --gc --minify",
  "outputDirectory": "public",
  "framework": "hugo"
}
